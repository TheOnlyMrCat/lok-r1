language: cpp

before-script:
  - ./.travis/install-automake.sh
script: ./autogen.sh && ./configure && make && make check && make distcheck

compiler:
- gcc
- clang

addons:
  apt:
    packages:
    - autoconf
    install:
    - flex