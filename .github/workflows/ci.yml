name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: configure
        run: ./autogen.sh
      - run: ./configure
      - name: make
        run: make
      - name: check
        run: make check
      - run: make distcheck