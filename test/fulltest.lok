namespace <clok::test>

use <std::string>
use <std::random>

fn sum <i32> [a <i32>; b <i32> = 0] {
	return a + b;
}

fn noop {
	# The thing doesn't even do anything!
}

run {
	one <i32>;

	one = 50;
	two <> = 4093871;

	two++;
	one--;
	one**;
	two//;

	result <i32> = sum [one, two];

	noop [];

	<bit:> strBytes = "Hello, world!";
	<string> strProper = strBytes; # Implicit constructor call string(<bit:>)
}

namespace <clok::test::sub>

class <type> {
	
}
