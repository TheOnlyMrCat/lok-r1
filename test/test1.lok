use <std::string>;
use <std:random>;

fn sum <i32> [a <i32>; b <i32> = 0] {
	return a + b;
}

fn noop {
	// The thing doesn't even do anything!
}

run {
	one <i32>;

	one = 50;
	two <> = 4093872;

	result <i32> = sum [one, two];

	noop;

	strBytes <byte:> = "Hello, world!";
	strProper <string> = strBytes; // Implicit constructor call string(<byte:>)
}
